# Phase 12 — Design Decisions & Bug Fixes

Date: 2026-07-02

**Scope:** Implements the four resolved Phase 12 design decisions (VP-12.1, VP-12.2 — decisions only, no implementation rows) across six implementation items: the `session_resumed` event with granular resume error codes and the resume-failure UI transition gap (VP-12.3), capacity display + `ROOM_FULL` copy (VP-12.4), `peers[].isHost` (VP-12.5), the WebRTC syncPeers stale-state race (VP-12.6), closed data-channel reuse in `startOffer` (VP-12.7), and the timer double-countdown (VP-12.8). Backlog sources: BL-DOC-CONTRACT-DRIFT-01, BL-SIG-ROOMFULL-GHOST-01, BL-RESUME-DEAD-ROOM-UI-01, BL-WEBRTC-SYNCPEERS-RACE-01, BL-WEBRTC-CLOSED-CHANNEL-REUSE-01, BL-UX-TIMER-DOUBLE-COUNTDOWN-01.

**Ordering note:** VP-12.5 (peers type tightening) and VP-12.3 (SessionResumedPayload) touch the same `shared/payloads.ts` peers type — implement 12.5's shared-type change first or together with 12.3 so `SessionResumedPayload` inherits the final peer shape. VP-12.4's `reconnectingCount` is additive to the same payloads and is order-independent. VP-12.6/12.7/12.8 are independent of everything else.

---

## Table of Contents

- [Constraints & Decisions](#constraints--decisions)
- [VP-12.3 Resume Session Event & Granular Error Codes](#vp-123-resume-session-event--granular-error-codes)
- [VP-12.4 Capacity Display & ROOM_FULL Copy](#vp-124-capacity-display--room_full-copy)
- [VP-12.5 Fix peers[].isHost in Join/Resume Payloads](#vp-125-fix-peersishost-in-joinresume-payloads)
- [VP-12.6 Fix WebRTC Sync-Peers Race](#vp-126-fix-webrtc-sync-peers-race)
- [VP-12.7 Fix Closed Data Channel Reuse](#vp-127-fix-closed-data-channel-reuse)
- [VP-12.8 Fix Timer Double-Countdown](#vp-128-fix-timer-double-countdown)

---

## Constraints & Decisions

| # | VP Item | Type | Detail | Rationale |
|---|---------|------|--------|-----------|
| 1 | VP-12.3 | Constraint | (I-12.3a) A successful resume must deliver to the resumer everything `room_joined` delivers today — `roomId`, `participantId`, `hostId`, `peers`, `expiresAt`, `soloDeadlineAt`, rotated `reconnectToken`, `participantNickname`, `participantCount`, `hasPassword`, `roomName`, plus the frontend chat-snapshot restore. Post-resume frontend state must be a strict superset of today's, adding only `hostReconnectGraceDeadlineAt`. | The event rename must not regress any restored session state; the payload builder at `registerSocketHandlers.ts:630-642` is renamed, not reduced. |
| 2 | VP-12.3 | Constraint | (I-12.3b) Events sent to other participants remain `peer_joined`. The resumer receives exactly one capability-negotiated response: `session_resumed` when `supportsSessionResumed === true`, otherwise legacy `room_joined`. | Capability negotiation preserves open Phase 11 clients without duplicating success events for new clients. |
| 3 | VP-12.3 | Constraint | (I-12.3c) The `INVALID_PASSWORD` emission on password-version mismatch (`registerSocketHandlers.ts:589-592`) is untouched. The 5-path error mapping replaces only the `emitRoomNotFound` calls **inside** the `resume_session` handler (`:565,573,578,585,596`); `join_room` / `leave_room` / signal-relay / kick `ROOM_NOT_FOUND` emissions are untouched. | Scoped blast radius: password rotation semantics and all non-resume `ROOM_NOT_FOUND` behavior are already contract-correct. |
| 4 | VP-12.3 | Constraint | On a terminal resume failure in auto-resume, reconnecting (including cold/ref-cleared), or the stale/host visible-room safeguard, capture the saved room ID, clear its chat snapshot and reconnect token, dispose the room session, and enter `room-ended` with mapped copy. Unknown/non-terminal reconnect errors retain the token and use the existing lobby fallback. | This resolves CR12-10 and CR12-15 without changing non-terminal behavior or putting teardown inside a React state updater. |
| 5 | VP-12.3 | Constraint | (I-12.3e) Rate-limiting and lock-ordering on the resume path are unchanged: the payload pre-check stays outside `withRoomLock`, all record/room/auth checks stay inside it, and no new await points are introduced inside the lock. | `withRoomLock` serializes resume against destroy/kick; reordering lock boundaries could reintroduce resume/destroy races. |
| 6 | VP-12.3 | Decision | The resume handler has **six** failure emit sites, not five. The two sites outside the named 5-path mapping are resolved as: (a) malformed payload (missing `roomId`/token, `:564-567`, pre-lock) **keeps `ROOM_NOT_FOUND`** — no valid token means no session to classify, and disclosure-avoidance argues against a more specific code; (b) reconnect record valid but participant absent from `room.participants` (`:594-598`, e.g. kicked/evicted during grace) → **`RECONNECT_TOKEN_STALE`** — the room lives but the session is unrecoverable, matching error-codes.md line 18 semantics. | Todo.md's "replace all emitRoomNotFound calls per the 5-path mapping" under-counts the sites; both extras need explicit, tested assignments to avoid an implementer guessing. |
| 7 | VP-12.3 | Decision | To classify "grace expired" as host vs guest, the `room`/`auth` lookup (`:582-587`) is **hoisted above** the disconnected/expired check (`:577-580`). New in-lock order: (1) record-hash check → `RECONNECT_TOKEN_STALE`; (2) `!room \|\| !auth` → `ROOM_NOT_FOUND`; (3) `!record.disconnected` → `RECONNECT_TOKEN_STALE`; (4) `now() > record.validUntil` → `room.hostId === record.participantId ? HOST_RECONNECT_WINDOW_EXPIRED : RECONNECT_TOKEN_STALE`; (5) password-version mismatch → `INVALID_PASSWORD` (unchanged); (6) `!participant` → `RECONNECT_TOKEN_STALE`. Side effect: a valid-hash token against a destroyed room now returns `ROOM_NOT_FOUND` even when the grace is also expired — which is exactly the mapped "room destroyed after valid token" outcome. | `room.hostId` is required for the host/guest split and is only available after the room lookup; hoisting is the minimal reorder that satisfies the mapping. Note: in the common case the host-grace timer destroys the room at the same instant `validUntil` passes, so `HOST_RECONNECT_WINDOW_EXPIRED` is reachable only in the timer-jitter window — the deterministic test must construct it with fake timers (advance clock past `validUntil` without firing the grace timer). |
| 8 | VP-12.3 | Decision | New instrumented emit helpers `emitReconnectTokenStale` / `emitHostReconnectWindowExpired` follow the existing closure-helper pattern (`registerSocketHandlers.ts:140-159`). `MetricsErrorCode` (`backend/src/admin/metrics.ts:7-12`) and the `errorCounts` initializer (`:55-61`) are extended with `RECONNECT_TOKEN_STALE` and `HOST_RECONNECT_WINDOW_EXPIRED`. | The file-level comment forbids un-instrumented emit twins; extending the metrics union is additive and keeps the admin error-count snapshot complete. |
| 9 | VP-12.3 | Constraint — **SPEC CONFLICT FLAG** | signaling-contract.md §3 (line 63) lists `session_resumed` **without** `reconnectToken`, but VP-12.1(1) defines `SessionResumedPayload` as extending `RoomJoinedPayload` (which has `reconnectToken`) and I-12.3a requires the rotated token on resume. **Resolution: the payload includes the rotated `reconnectToken`; signaling-contract.md §3 line 63 is corrected in this phase.** | Dropping token rotation from resume would break the sessionStorage refresh loop (the old token is invalidated by `upsertReconnectToken` at `:612-617`); the contract line is the drifted artifact. |
| 10 | VP-12.3 | Constraint — **SPEC DRIFT FLAG** | lifecycle.md §5 resume sequence diagrams (lines 183 and 214) still show `room_joined` as the resume response, contradicting lifecycle.md §1 Rule 6 and §3 (`session_resumed`). Both diagram lines are updated to `session_resumed` in this phase. The §5 session-persistence flowchart (`:233-240`) is also extended to show `RECONNECT_TOKEN_STALE` / `HOST_RECONNECT_WINDOW_EXPIRED` as failure outcomes. | The normative doc must be internally consistent once the event exists at runtime. |
| 11 | VP-12.3 | Decision | `SessionResumedPayload.hostReconnectGraceDeadlineAt?: number` is populated from `policy.hostGraceDeadlineAt` (`graceWindowManager.ts:7`) and is present **only when a host grace window is active at emit time** — i.e., a guest resuming while the host is in grace. A resuming host clears their own grace first (`:603-606`), so the field is absent on host self-resume; it is also absent when no grace is running. | This is the entire reason `session_resumed` exists (VP-12.1): a resuming guest otherwise never learns the host grace deadline because `host_reconnect_grace` was broadcast while they were offline. |
| 12 | VP-12.3 | Decision | `onError` uses one terminal-resume handler before its state updater: all five terminal codes in automatic/reconnecting contexts, plus the stale/host visible-room safeguard, clear the exact stored chat key, token, and mesh, then use `withRoomEnded` with mapped error copy. Duplicate terminal errors preserve `room-ended`. The conflicting normative wording about a lobby destination is retained as a documentation-alignment follow-up and is not silently edited here. | The common handler is idempotent in its cleanup and prevents React ref timing from changing the user-visible fatal outcome. |
| 13 | VP-12.4 | Constraint | (I-12.4a) The capacity gate stays on `room.participants.size >= MAX_PARTICIPANTS_PER_ROOM` (`registerSocketHandlers.ts:468-471`); join acceptance/rejection is bit-for-bit identical. Grace slots are reserved capacity per the VP-12.2 decision. The entire change is display-side. | Gating on liveCount is provably broken: `resume_session` replaces sentinels in-place with no capacity check, so freed "live" slots plus resumes overflow the 5-person cap (VP-12.2 analysis). |
| 14 | VP-12.4 | Constraint | (I-12.4b) `reconnectingCount` is additive and optional (`reconnectingCount?: number`); existing consumers of `room_joined`/`peer_joined`/`peer_left` and the meaning of every existing count field (`participantCount` = live count) are unchanged. | Optional typing keeps old clients/tests valid; no field is repurposed. |
| 15 | VP-12.4 | Constraint | (I-12.4c) Only the `ROOM_FULL` entry in `error-copy.ts` (`:35-36`) changes. The `ErrorCode` type (`types.ts:52-62`) and all other `getErrorMessage` entries stay as-is — verified complete against error-codes.md's 9-code set. | Todo.md note confirmed by source inspection: no other copy work exists. |
| 16 | VP-12.4 | Decision | `reconnectingCount` is always emitted and tracked separately from live participants. The frontend normalizes absent/malformed values to zero and renders `N connected · M reconnecting` only when `M > 0`; disconnected sentinels never enter the live roster. | This surfaces reserved capacity while preserving lifecycle.md's live-only roster rule. |
| 17 | VP-12.4 | Constraint — lockstep flag | frontend-ui-spec.md §9.4 (line 279: `ROOM_FULL -> Room is full (5 max).`) owns the copy mapping per error-codes.md's header and must be updated to the new string in the same change. signaling-contract.md §3/§5 must gain the `reconnectingCount?` field on the three payloads, or the fix creates fresh doc/code drift of the exact kind VP-12.1 just resolved. | Copy and payload shape are single-homed in the normative set; code-only changes would recreate BL-DOC-CONTRACT-DRIFT-01. |
| 18 | VP-12.5 | Constraint | (I-12.5a, I-12.5b) Peers array ordering and filtering (excludes self on the resume path, excludes `disconnected:` sentinels on both paths) are unchanged — only the per-entry shape gains `isHost: peer.participantId === room.hostId`, computed at emit time. At most one entry carries `isHost: true`; when the host is a filtered sentinel or the joiner themself, zero entries do. | `room.hostId` is in scope at both map sites (`roomLifecycle.ts:70-75`, `registerSocketHandlers.ts:622-624`); the filter predicates are load-bearing for I-12.5b (a host sentinel must not yield a phantom `isHost: true` entry). |
| 19 | VP-12.5 | Constraint | (I-12.5c) Host-badge rendering must be visually identical. Since the server computes `isHost` from the same `room.hostId` it sends as `payload.hostId`, `peer.isHost` is definitionally equal to today's client-side `participant.participantId === payload.hostId` derivation in `withRoomJoined` (`state-utils.ts:79-86`). Render sites (`RoomView.tsx:171,196,506`; `RoomViewDesktop.tsx:354,532,557`) already consume `Participant.isHost` from state and need no changes. | The frontend `Participant` interface already has `isHost` (`types.ts:74-77`); the change is only *where* the flag originates (wire vs derivation). |
| 20 | VP-12.5 | Constraint | (I-12.5d) Tightening `nickname?: string \| null` → `nickname: string \| null` in the peers type must survive typecheck at every construction site. Both existing sites already emit `nickname: peer.nickname ?? null` explicitly (`roomLifecycle.ts:74`, `registerSocketHandlers.ts:624`), so the gate is `tsc -b` clean in both workspaces plus the test files. | Verified: no construction site omits the property, so the tightening is a pure type-level truth-alignment. |
| 21 | VP-12.6 | Constraint | (I-12.6a) On `peer_left`, `syncPeers` must still prune the departed peer's connection — repair semantics preserved. Only the *source* of `remainingPeerIds` moves from the stale `stateRef.current.participants` snapshot (`useVaporRoom.ts:333-339`) to the post-`withPeerLeft` state inside the updater. `withPeerLeft` already filters the leaver (`state-utils.ts:135-141`), so the derived set needs only the self-exclusion filter. The existing `remainingPeerIds.length > 0` skip (`:345`) is preserved. | The stale-ref read is the entire bug; the pruning behavior is the entire feature (VP-10.2). |
| 22 | VP-12.6 | Decision | (I-12.6b) State updaters remain pure. `onPeerLeft` sets `peerRepairPendingRef` before enqueueing state; a commit-phase effect clears the marker first, derives peers from committed participants, and calls `syncPeers` once. Batched leaves collapse to one repair of the final committed set. | Commit-phase flushing is safe under updater replay, abandoned renders, rebasing, batching, and StrictMode effect replay. |
| 23 | VP-12.6 | Constraint | (I-12.6c) The rest of `onPeerLeft` is order- and behavior-unchanged: `peerMeshRef.current?.handlePeerLeft` stays first, the system chat message (`"{name} disconnected/was removed/left"`), `soloDeadlineAt` adoption, pending-message clearing, `connectedPeerCount`/`chatConnectionState` transitions, and mesh-driven typing cleanup all keep their current sequence. | These behaviors were individually specified in VP-10.x/VP-11.6 and have existing tests; the fix must be surgical. |
| 24 | VP-12.7 | Constraint | (I-12.7a) `startOffer`'s duplicate-guard for healthy channels remains: an `'open'` or `'connecting'` channel still short-circuits channel creation. Only `'closed'`/`'closing'` entries are evicted **before** the `!this.dataChannels.has(peerId)` guard (`webrtc-chat-mesh.ts:370`). | The guard exists to prevent duplicate channels during normal negotiation (CR10-3); eviction must be state-conditional, not unconditional. |
| 25 | VP-12.7 | Decision | Eviction detaches the dead channel's handlers (`onopen`/`onclose`/`onerror`/`onmessage` = null) and deletes it from `dataChannels` — it does **not** call `removePeer`, because the `RTCPeerConnection` is healthy and must survive for the renegotiated offer (that is the repair being performed). Calling `channel.close()` on an already-closed channel is a harmless no-op and may be included for the `'closing'` case. | `removePeer` (`:404-424`) tears down the connection too, which would defeat `needsOffer`'s repair intent; the backlog's alternative fix (`removePeer` from `onclose`) is explicitly not chosen. |
| 26 | VP-12.7 | Constraint | (I-12.7b) `connectedPeerCount` derivation (counts only `'open'` channels, `:426-437`) and the perfect-negotiation politeness rules (`shouldInitiate` lexicographic comparison `:241-243`, rollback handling in `handleSignalOffer`) are unchanged. | The fix is scoped to the channel-map hygiene inside `startOffer`; negotiation topology is untouched. |
| 27 | VP-12.8 | Constraint | (I-12.8a) `SoloWaitingChip` remains the exclusive surface for `soloDeadlineAt`: `derived.soloDeadlineAt` (`useVaporRoom.ts:709`) and `getSoloWaitingText` (`:102-114`) are untouched. (I-12.8c) `RoomLifetimeChip` countdown cadence and deadline ownership are unchanged; its formatting follows frontend-ui-spec.md §4/§9.5: compact minutes only above 10 minutes, and `mm:ss` at 10 minutes or less. When `expiresAt` is null the chip renders nothing. | The deadline-source fix must not change cadence or ownership. The exact 10:00 boundary is aligned to the normative UI specification. |
| 28 | VP-12.8 | Constraint | (I-12.8b) Active host grace has a distinct persistent banner with a second-ticking countdown in both room layouts. It is driven only by `hostReconnectGraceDeadlineAt` and disappears when the host returns or the deadline is absent/invalid/expired. | This directly satisfies frontend-ui-spec.md §§3D/4 without changing the TTL countdown. |
| 29 | VP-12.8 | Constraint | lifecycle.md and frontend-ui-spec.md agree on one surface per deadline: TTL chip, solo chip, and host-grace countdown banner. | The previous static-status interpretation weakened the normative countdown requirement and is superseded. |
| 30 | VP-12.8 | Decision | `derived.expiresAt` becomes `state.expiresAt` directly; the `effectiveExpiresAt` `useMemo` (`useVaporRoom.ts:656-661`) is deleted rather than reduced to a one-input `Math.min`. | A single-source pass-through needs no memo; keeping a degenerate memo invites the next author to re-add inputs. |

---

## VP-12.3 Resume Session Event & Granular Error Codes

*(BL-DOC-CONTRACT-DRIFT-01 drifts (1)+(4), BL-RESUME-DEAD-ROOM-UI-01)*

### Implementation Matrix

| Subtask | Task | Module / Interface | Detail | Status | Pass Criteria | Evidence Pointer |
|---------|------|--------------------|--------|--------|---------------|-----------------|
| 12.3.1 | Add `SESSION_RESUMED` server event name | `shared/events.ts` | Add `SESSION_RESUMED: "session_resumed"` to `SERVER_EVENT_NAMES` (after `ROOM_JOINED`). | Done | `SERVER_EVENT_NAMES.SESSION_RESUMED === "session_resumed"`; typecheck clean. | `shared/events.ts:13-26` |
| 12.3.2 | Add `SessionResumedPayload` type | `shared/payloads.ts` | `export type SessionResumedPayload = RoomJoinedPayload & { hostReconnectGraceDeadlineAt?: number }`. Declared after `RoomJoinedPayload` so it inherits the VP-12.5-tightened peers shape and the VP-12.4 `reconnectingCount?`. Includes `reconnectToken` by inheritance (Constraints #9). | Done | Type exported; extends `RoomJoinedPayload`; `tsc -b` clean in both workspaces. | `shared/payloads.ts:82-94` |
| 12.3.3 | Re-export via backend contracts | `backend/src/signaling/contracts.ts` | Add `sessionResumed` to the `SERVER_EVENTS` map and `SessionResumedPayload` to the type re-exports, following the existing `roomJoined` pattern. | Done | `signaling.SERVER_EVENTS.sessionResumed` resolves to `"session_resumed"`; type importable. | `contracts.ts` SERVER_EVENTS / type re-export blocks |
| 12.3.4 | Emit a capability-negotiated resume response | `shared/payloads.ts`, `frontend/src/features/room/useVaporRoom.ts`, `backend/src/signaling/registerSocketHandlers.ts` | Add optional `supportsSessionResumed`; new clients send `true`. Emit `session_resumed` only for true, otherwise legacy `room_joined`; emit exactly one response. | Done | New/new, old/new, and new/old rollout combinations retain resume state without duplicate success handling. | `shared/payloads.ts`, `useVaporRoom.ts`, `registerSocketHandlers.ts` |
| 12.3.5 | Add instrumented error helpers + metrics codes | `registerSocketHandlers.ts:140-159`, `backend/src/admin/metrics.ts:7-12,55-61` | Add `emitReconnectTokenStale` / `emitHostReconnectWindowExpired` closure helpers mirroring `emitRoomNotFound`. Extend `MetricsErrorCode` union and `errorCounts` initializer with both codes (Constraints #8). | Done | Both helpers increment their counter and emit the shared `SocketErrorPayload`; `/admin/metrics` snapshot includes zero-initialized new counters; typecheck clean. | `registerSocketHandlers.ts:140-159`, `metrics.ts:7-12,55-61` |
| 12.3.6 | Rewire the six resume-failure sites per mapping | `registerSocketHandlers.ts:558-598` | Apply Constraints #6/#7 ordering: pre-lock malformed payload keeps `emitRoomNotFound` (`:564-567`); in-lock — record-hash miss → `emitReconnectTokenStale` (`:572-575`); hoisted `!room \|\| !auth` → `emitRoomNotFound`; `!record.disconnected` → `emitReconnectTokenStale`; `now() > record.validUntil` → host ? `emitHostReconnectWindowExpired` : `emitReconnectTokenStale`; `INVALID_PASSWORD` site unchanged; `!participant` → `emitReconnectTokenStale`. No new awaits inside the lock (I-12.3e). | Done | Each of the six paths emits its mapped code (see T12.3-01…06, -16); success path unchanged; `join_room`/`leave_room`/relay `ROOM_NOT_FOUND` sites untouched (grep confirms `emitRoomNotFound` unchanged outside resume). | `registerSocketHandlers.ts:558-598` |
| 12.3.7 | Frontend contract wiring — types | `frontend/src/features/room/types.ts` | Add `SESSION_RESUMED` to `SERVER_EVENTS` (`:37-50`); alias `SessionResumedPayload` from `@shared`; add `onSessionResumed`/`offSessionResumed` to `RoomSocketClient` (`:192-229`). `ErrorCode` union already contains both resume codes (`:52-62`) — no change. | Done | Types compile; interface members present; no `ErrorCode` edits. | `types.ts:37-50,192-229` |
| 12.3.8 | Frontend contract wiring — socket client | `frontend/src/features/room/room-socket-client.ts` | Register `onSessionResumed`/`offSessionResumed` against `SERVER_EVENTS.SESSION_RESUMED`, following the `onRoomJoined` pattern (`:37,52`). | Done | Client registers/deregisters the listener; typecheck clean. | `room-socket-client.ts:33-74` |
| 12.3.9 | Frontend contract wiring — connection hook | `frontend/src/features/room/hooks/useSocketConnection.ts` | Add `onSessionResumed: (payload: SessionResumedPayload) => void` to `SocketEventHandlers` (`:17-31`); add the stable wrapper + register/deregister pair (`:65-96`). | Done | Handler wired symmetrically with the other 13 handlers. | `useSocketConnection.ts:17-31,47-97` |
| 12.3.10 | State reducer `withSessionResumed` | `frontend/src/features/room/state-utils.ts` | `withSessionResumed(state, payload)` = `withRoomJoined(state, payload)` result with `hostReconnectGraceDeadlineAt: payload.hostReconnectGraceDeadlineAt ?? null` applied after (must override `clearSessionFields`' null). | Done | Resulting state is a strict superset of `withRoomJoined` output, differing only in `hostReconnectGraceDeadlineAt` (I-12.3a); unit test T12.3-11. | `state-utils.ts:78-115,249-275` |
| 12.3.11 | `onSessionResumed` handler | `frontend/src/features/room/useVaporRoom.ts` | Mirror `onRoomJoined` (`:282-309`) exactly — `persistence.writeStoredReconnectSession` (rotated token), reset `resumeInFlightRef`/`autoResumeRequestedRef`, `chat.clearPending()`, `loadChatHistory` merge, `createPeerMesh` + `syncPeers(payload.peers.map(...))`, `requestPermission` — but through `withSessionResumed`. Register in the `useSocketConnection` handlers object (`:502-521`). `onRoomJoined` itself is retained unchanged for create/join flows. | Done | Resume restores full room state incl. grace deadline and chat snapshot; peer mesh syncs against resume peers; stored token rotated. | `useVaporRoom.ts:282-309,502-521` |
| 12.3.12 | Initial visible-room resume-error guard | `frontend/src/features/room/useVaporRoom.ts` | The original narrow stale/host visible-room guard established the `withRoomEnded` outcome. It is now superseded by the centralized terminal handler in 12.3.16. | Superseded | The visible-room edge remains covered by the common terminal-resume behavior. | `useVaporRoom.ts` |
| 12.3.13 | Normative doc alignment | `docs/system_design/error-codes.md`, `signaling-contract.md`, `lifecycle.md`, `core-architecture.md`, `frontend-ui-spec.md` | Align the granular failure mapping, complete resume payload, capability-negotiated success response, host-grace deadline, and session-persistence behavior across the normative set. | Done | Current clients use `session_resumed`; legacy `room_joined` is documented only as the absent-capability fallback; resume failures no longer collapse to `ROOM_NOT_FOUND`. | Named normative documents |
| 12.3.14 | Align frontend admin metrics contract | `frontend/src/features/admin/adminApi.ts` | Add both Phase 12 resume-error counters to the exported frontend metrics key list/union. | Done | Frontend and backend snapshots expose the same seven error-count keys. | `adminApi.ts`, `admin.unit.test.ts`, `phase12.behavior.test.tsx` |
| 12.3.15 | Clear the cold-reconnect chat snapshot on terminal resume failure (CR12-10) | `frontend/src/features/room/useVaporRoom.ts` | The centralized terminal handler reads the stored room ID before deleting the reconnect record and removes `vapor.chat:<roomId>` even when `activeRoomId` is still null. | Implemented - runtime test deferred | A cold reconnect does not leave a terminal-room chat snapshot after its reconnect record is cleared. Frontend lint has 0 errors (3 pre-existing warnings) and `npm.cmd run build --prefix frontend` passes. | `useVaporRoom.ts`; focused runtime storage-key test deferred by request |
| 12.3.16 | Normalize terminal resume-failure outcome (CR12-15) | `frontend/src/features/room/useVaporRoom.ts` | The same terminal handler covers automatic resume, cold/ref-cleared reconnecting, and stale/host failures while a room is visible. It clears the session and enters `room-ended` with mapped error copy; unknown/non-terminal reconnect errors retain the saved record and fall back to lobby. | Implemented - runtime test deferred | React ref timing no longer changes a terminal resume failure from an explained Room Ended state to a silent lobby. Frontend lint has 0 errors (3 pre-existing warnings) and `npm.cmd run build --prefix frontend` passes. | `useVaporRoom.ts`; targeted runtime test and full-suite rerun deferred by request |

### Test Plan

**Execution status: historical Pass - full-suite rerun completed 2026-07-30 before the CR12-10/CR12-15 remediation.** Targeted runtime tests and a full-suite rerun are deferred by request; the recorded results do not validate the current remediation.

| Test # | Suite | Purpose | Verification Focus | Status |
|--------|-------|---------|-------------------|--------|
| T12.3-01 | integration (backend `socket.integration.test.ts`) | Resume path 1: token hash not found → `RECONNECT_TOKEN_STALE` | Emit `resume_session` with a syntactically valid but never-issued token (and separately: a real token against a different `roomId`); assert `error.code === "RECONNECT_TOKEN_STALE"`, no `session_resumed`, metrics counter incremented. | Pass |
| T12.3-02 | integration (backend) | Resume path 2: participant not disconnected → `RECONNECT_TOKEN_STALE` | Resume with the current valid token of a **still-connected** participant (record `disconnected === false`); assert `RECONNECT_TOKEN_STALE`, room state untouched. | Pass |
| T12.3-03 | integration (backend, injected clock) | Resume path 3: grace expired + host → `HOST_RECONNECT_WINDOW_EXPIRED` | Host + guest; host TCP-drops (grace armed, `validUntil` set); advance injected `now()` past `validUntil` **without** firing the host-grace timer (jitter window, Constraints #7); host resumes → assert `HOST_RECONNECT_WINDOW_EXPIRED`. | Pass |
| T12.3-04 | integration (backend, injected clock) | Resume path 4: grace expired + guest → `RECONNECT_TOKEN_STALE` | Host + guest; guest drops; advance `now()` past guest `validUntil` without firing timers; guest resumes → assert `RECONNECT_TOKEN_STALE` (not `HOST_RECONNECT_WINDOW_EXPIRED`). | Pass |
| T12.3-05 | integration (backend) | Resume path 5: room destroyed after valid token → `ROOM_NOT_FOUND` | Construct a live reconnect record whose room has been removed (delete `state.rooms` entry while retaining the reconnect record via the harness); resume → assert `ROOM_NOT_FOUND`, even when the record's grace is also expired (hoisting precedence, Constraints #7). | Pass |
| T12.3-06 | integration (backend) | Password-version mismatch still → `INVALID_PASSWORD` | Guest drops; host rotates password (`room_password_update`); guest resumes with pre-rotation token → assert `INVALID_PASSWORD` exactly as today; ordering relative to the new checks unchanged. | Pass |
| T12.3-07 | integration (backend) | Successful resume payload completeness + token rotation | Guest drops and resumes; assert `session_resumed` (not `room_joined`) received with: `roomId`, `participantId` (original identity), `hostId`, `peers` (live-only, each with `participantId`/`nickname`/`isHost`), rotated `reconnectToken` (≠ old token; old token subsequently rejected), `expiresAt`, `participantNickname` (reserved nickname reclaimed), `participantCount`, `hasPassword`, `roomName`, `soloDeadlineAt` present iff live count ≤ 1 (I-12.3a). | Pass |
| T12.3-08 | integration (backend) | `hostReconnectGraceDeadlineAt` presence semantics | (a) Guest resumes **while host is in grace** → field present and equals `policy.hostGraceDeadlineAt`; (b) host resumes → grace cleared first, field absent; (c) guest resumes with no grace active → field absent (Constraints #11). | Pass |
| T12.3-09 | integration (backend) | Peer re-announcement unchanged (I-12.3b) | On successful resume, remaining participants receive exactly one `peer_joined` with the same shape/fields as before this phase (participantId, nickname, participantCount, reconnectingCount per VP-12.4); no `session_resumed` is broadcast to them. | Pass |
| T12.3-10 | integration (existing — backend) | Non-resume `ROOM_NOT_FOUND` emissions untouched (I-12.3c) | Existing `join_room` unknown-room, kick unknown-target, and signal-relay dead-room tests pass unchanged; grep confirms no error-code change outside the resume handler. | Pass |
| T12.3-11 | unit (frontend — `stateUtils.unit.test.mjs`) | `withSessionResumed` is a superset of `withRoomJoined` | Given identical payload minus grace field, outputs are deep-equal except `hostReconnectGraceDeadlineAt`; with grace field present it lands in state; absent → `null` (not `undefined`). | Pass |
| T12.3-12 | unit (frontend — `phase12.behavior.test.tsx`) | Uncaught edge: `screen === 'room'`, ref already cleared → `withRoomEnded` guard | Render `useVaporRoom`, drive the captured socket `onRoomJoined` then each fatal `onError`, and assert room-ended state, cleared participant/chat state, and storage clearing. | Pass |
| T12.3-13 | unit (frontend runtime) | Automatic terminal resume ends the room consistently | Start automatic resume with a saved session; for each terminal code, assert exact chat-key/token removal, mesh disposal, mapped `room-ended` copy, and idempotence under a duplicate error. Assert an unknown error preserves the saved record and follows the lobby fallback. | Deferred - test update not requested |
| T12.3-14 | unit (frontend runtime) | Cold/ref-cleared terminal resume ends the room consistently | Start in cold reconnect state with stored token and `vapor.chat:<roomId>`; drive each terminal error and assert the exact key is removed before storage is cleared, Room Ended is shown, and non-terminal errors retain the record. | Deferred - test update not requested |
| T12.3-15 | integration (existing — backend) | Rate-limiting and lock-ordering on resume unchanged (I-12.3e) | Existing `withRoomLock` serialization and resume/destroy-race coverage is retained. Legacy successful-resume assertions consume `session_resumed`; the injected-clock expired-host assertion expects `HOST_RECONNECT_WINDOW_EXPIRED`. | Pass |
| T12.3-16 | integration (backend) | Sixth site: malformed payload keeps `ROOM_NOT_FOUND`; evicted participant → `RECONNECT_TOKEN_STALE` | (a) `resume_session` with missing/empty token or missing roomId → `ROOM_NOT_FOUND` (Constraints #6a); (b) guest drops into grace, host kicks the sentinel, guest resumes with the still-hashed token whose record survived → `RECONNECT_TOKEN_STALE` (Constraints #6b; if kick clears the record, the path is exercised by direct roster deletion via the harness). | Pass |
| T12.3-17 | build (typecheck + doc grep) | Wiring exhaustive; docs aligned | `tsc -b` clean in backend + frontend; grep: `session_resumed` present in `shared/events.ts`, `contracts.ts`, `types.ts`, `room-socket-client.ts`, `useSocketConnection.ts`, `useVaporRoom.ts`; error-codes.md line 13 no longer contains `liveCount === 0`; lifecycle.md §5 diagrams contain no `room_joined` on resume; signaling-contract.md §3 `session_resumed` lists `reconnectToken`. | Pass |
| T12.3-18 | integration (backend + frontend runtime) | Rolling-deployment compatibility | Backend: omit capability and assert exactly one legacy `room_joined`; set capability and assert exactly one `session_resumed`. Frontend: automatic resume advertises `supportsSessionResumed: true`. | Pass |
| T12.3-19 | unit (backend + frontend runtime) | Metrics key lockstep | Assert backend snapshots and the frontend exported key list contain both resume error counters. | Pass |
| T12.3-20 | unit (frontend runtime) | CR12-10 storage-key regression | Seed a cold reconnect record and matching per-room chat history, invoke a terminal error after the auto-resume ref is cleared, and assert both storage keys are removed and the state is `room-ended`. | Deferred - test update not requested |
| T12.3-21 | unit (frontend runtime) | CR12-15 outcome consistency | Compare automatic, cold/ref-cleared, and visible-room terminal error flows; assert all finish in `room-ended` with the mapped message and no live mesh/pending chat. | Deferred - test update not requested |

---

## VP-12.4 Capacity Display & ROOM_FULL Copy

*(BL-SIG-ROOMFULL-GHOST-01 — display-side fix per VP-12.2; gate unchanged)*

### Implementation Matrix

| Subtask | Task | Module / Interface | Detail | Status | Pass Criteria | Evidence Pointer |
|---------|------|--------------------|--------|--------|---------------|-----------------|
| 12.4.1 | Add `reconnectingCount?` to shared payloads | `shared/payloads.ts` | Add `reconnectingCount?: number` to `RoomJoinedPayload` (`:82-94`), `PeerJoinedPayload` (`:96-100`), `PeerLeftPayload` (`:102-107`). `SessionResumedPayload` inherits it via 12.3.2. | Done | Fields present, optional; typecheck clean; no existing payload construction breaks. | `shared/payloads.ts:82-107` |
| 12.4.2 | Backend derivation helper + join-path emission | `registerSocketHandlers.ts` | Add closure helper `getReconnectingCount(room) = room.participants.size - getLiveParticipantCount(room)`. Include `reconnectingCount` (always emitted, 0 allowed — Constraints #16) in the join `roomJoinedPayload` (`:502-516`) and `peerJoinedPayload` (`:518-524`). Capacity gate at `:468-471` untouched (I-12.4a). | Done | Both join-path payloads carry the value; gate logic diff is empty. | `registerSocketHandlers.ts:468-524` |
| 12.4.3 | Resume-path emission | `registerSocketHandlers.ts:630-650` | Include `reconnectingCount` in the `session_resumed` payload and the resume `peer_joined` broadcast. | Done | Both resume-path payloads carry the value. | `registerSocketHandlers.ts:630-650` |
| 12.4.4 | `peer_left` emission at all three construction sites | `registerSocketHandlers.ts:84-108,273-287,787-793,804-812` | `emitPeerLeftOnDisconnect`: compute and include. Kick `peerLeftPayload`: compute and include. Leave path: extend `emitParticipantExit` `extras` with `reconnectingCount` and populate it in the leave handler where `remainingRoom` is in scope. | Done | Every `peer_left` on an active room carries `reconnectingCount`; disconnect increments it (leaver becomes sentinel), explicit leave does not (leaver fully removed). | `registerSocketHandlers.ts:84-108,273-287,787-812` |
| 12.4.5 | Frontend state consumption and capacity display | `state-utils.ts`, `App.tsx`, `RoomView.tsx`, `RoomViewDesktop.tsx`, `participant-utils.ts` | Normalize the count across membership transitions, pass it to both layouts, and show it separately from the live roster. | Done | Absent/malformed values become zero; `M > 0` renders `N connected · M reconnecting`; roster membership is unchanged. | `state-utils.ts`, `App.tsx`, both room views |
| 12.4.6 | `ROOM_FULL` copy + doc lockstep | `frontend/src/features/room/error-copy.ts:35-36`, `docs/system_design/frontend-ui-spec.md:279`, `signaling-contract.md` §3/§5 | `getErrorMessage(ROOM_FULL)` returns `"Room is at capacity — some slots are held for reconnecting participants."`; no other entry changes (I-12.4c). Update frontend-ui-spec.md §9.4 line 279 to the same string; add `reconnectingCount?` to the three payload listings in signaling-contract.md §3 and a §5 alignment bullet (Constraints #17). | Done | Copy matches VP-12.2 exactly; docs and code agree; grep finds the old `"Room is full (5 max)."` only in historical/test-archive files. | `error-copy.ts:35-36`, `frontend-ui-spec.md:279`, `signaling-contract.md:59-115` |

### Test Plan

**Execution status: Pass — fresh full-suite rerun confirmed 2026-07-30.** All mapped Phase 12 tests pass; see `docs/test_results/phase-12-test-result.md`.

| Test # | Suite | Purpose | Verification Focus | Status |
|--------|-------|---------|-------------------|--------|
| T12.4-01 | integration (new — backend) | `room_joined.reconnectingCount === 0` with no grace-held slots | Fresh room, second participant joins; assert `room_joined.reconnectingCount === 0` and `peer_joined.reconnectingCount === 0` at the host. | Pass |
| T12.4-02 | integration (new — backend) | `room_joined.reconnectingCount > 0` with a sentinel present | Host + G1 + G2; G1 disconnects (grace sentinel); G3 joins; assert G3's `room_joined.reconnectingCount === 1` and `participantCount` still counts live only (I-12.4b). | Pass |
| T12.4-03 | integration (new — backend) | `peer_left` carries `reconnectingCount` for disconnect vs leave | (a) Guest disconnect → remaining clients' `peer_left.reconnectingCount === 1` (sentinel held); (b) guest explicit `leave_room` → `reconnectingCount === 0` (slot fully released); (c) kick of a live guest → `0`. | Pass |
| T12.4-04 | integration (new — backend) | Resume path decrements: `peer_joined.reconnectingCount` after `session_resumed` | G1 in grace (`count 1`); G1 resumes; assert resume `peer_joined.reconnectingCount === 0` and the resumer's `session_resumed.reconnectingCount === 0`. | Pass |
| T12.4-05 | integration (new — backend) | Capacity gate unchanged at boundary **with** grace-held slots (I-12.4a) | 3 live + 2 sentinels (`participants.size === 5 === MAX`); new joiner with a fresh nickname → `ROOM_FULL` emitted; roster and sentinels untouched. | Pass |
| T12.4-06 | integration (new — backend) | Capacity gate unchanged at boundary **without** sentinels | 5 live → 6th join rejected `ROOM_FULL`; 4 live → 5th join accepted. Acceptance/rejection behavior bit-for-bit identical to pre-phase (rerun of existing capacity tests plus the new assertions). | Pass |
| T12.4-07 | unit (frontend — error-copy) | `ROOM_FULL` copy updated; all other copy frozen (I-12.4c) | `getErrorMessage(ROOM_FULL)` equals the new string; snapshot-assert every other `ErrorCode` message is unchanged; `mapErrorCode` unchanged. | Pass |
| T12.4-08 | unit (frontend — `phase12.behavior.test.tsx`) | State consumption: absent/zero vs > 0 across all events | Execute `createInitialRoomSessionState`, `withRoomJoined`/`withSessionResumed` absent/nonzero payloads, then `withPeerJoined`/`withPeerLeft` zero/nonzero replacement payloads. Source-text checks are supplemental only. | Pass |

---

## VP-12.5 Fix peers[].isHost in Join/Resume Payloads

*(BL-DOC-CONTRACT-DRIFT-01 drift (3) — code-only; signaling-contract.md §3/§5 already correct)*

### Implementation Matrix

| Subtask | Task | Module / Interface | Detail | Status | Pass Criteria | Evidence Pointer |
|---------|------|--------------------|--------|--------|---------------|-----------------|
| 12.5.1 | Add `isHost` in `joinRoomRecord` peers map | `backend/src/signaling/roomLifecycle.ts:50-83` | In the peers `.map()` (`:70-75`) add `isHost: peer.participantId === room.hostId`; update the function's inline return type (`:56`) to `Array<{ participantId: string; nickname: string \| null; isHost: boolean }>` (nickname tightened, I-12.5d). Filter predicate unchanged (I-12.5a). | Done | Join `peers[]` entries each carry correct `isHost`; sentinel filter untouched; typecheck clean. | `roomLifecycle.ts:56,70-75` |
| 12.5.2 | Add `isHost` in resume peers map | `registerSocketHandlers.ts:622-624` | Add `isHost: peer.participantId === room.hostId` to the inline map (`room.hostId` in scope). Self- and sentinel-exclusion filters unchanged. | Done | Resume `peers[]` entries carry correct `isHost`; a host-sentinel yields no entry at all (I-12.5b). | `registerSocketHandlers.ts:622-624` |
| 12.5.3 | Tighten shared peers type | `shared/payloads.ts:86` | `RoomJoinedPayload.peers`: `Array<{ participantId: string; nickname?: string \| null }>` → `Array<{ participantId: string; nickname: string \| null; isHost: boolean }>`. `SessionResumedPayload` inherits. | Done | Both construction sites (`roomLifecycle.ts:74`, `registerSocketHandlers.ts:624`) already emit `nickname: … ?? null` — typecheck confirms no site omits either property. | `shared/payloads.ts:86` |
| 12.5.4 | Frontend consumes wire `isHost` | `frontend/src/features/room/state-utils.ts:78-95` | In `withRoomJoined` (shared by `withSessionResumed`), map `isHost: participant.isHost` instead of deriving from `payload.hostId` (`:81`). Self-entry push (`:84-86`) keeps its `payload.participantId === payload.hostId` derivation (self is never in `peers`). `withPeerJoined` (`:120`) keeps the `state.hostId` derivation — `peer_joined` carries no `isHost` and the contract does not add one. Render sites need no change (Constraints #19). | Done | `Participant.isHost` sourced from wire on join/resume; host badge rendering pixel-identical (I-12.5c). | `state-utils.ts:78-95,117-133` |
| 12.5.5 | Typecheck gate | Root | `npm run typecheck` in `backend/` and `frontend/`; fix any test-harness payload literals that omit `isHost`/`nickname`. | Done | `tsc -b` exits 0 in both workspaces including test files (I-12.5d). | Typecheck output |

### Test Plan

**Execution status: Pass — fresh full-suite rerun confirmed 2026-07-30.** All mapped Phase 12 tests pass; see `docs/test_results/phase-12-test-result.md`.

| Test # | Suite | Purpose | Verification Focus | Status |
|--------|-------|---------|-------------------|--------|
| T12.5-01 | integration (new — backend) | Exactly-one-host: join `peers[]` carries `isHost` agreeing with `hostId` | Host + G1; G2 joins; assert G2's `peers` has exactly one `isHost: true` entry, and it is the entry whose `participantId === room_joined.hostId` (I-12.5b). | Pass |
| T12.5-02 | integration (new — backend) | Host-in-grace: sentinel filtered; zero `isHost: true` entries | Host disconnects (sentinel); G2 resumes (or a new guest joins); assert `peers[]` contains no host entry at all and no entry has `isHost: true` — the sentinel filter must run before any host flagging. | Pass |
| T12.5-03 | integration (new — backend) | Resume-path peers carry `isHost` | Guest drops and resumes with host + another guest live; assert `session_resumed.peers` entries carry correct `isHost` (host true, guest false) and `nickname: string \| null` (never `undefined`). | Pass |
| T12.5-04 | build (typecheck) | Nickname tightening breaks no construction site (I-12.5d) | `tsc -b` clean both workspaces; grep for `peers:` payload literals in tests updated to the new triple shape. | Pass |
| T12.5-05 | unit (frontend — `phase12.behavior.test.tsx`) | `withRoomJoined` uses wire `isHost`; output equivalence | Execute `withRoomJoined` with valid and deliberately contradictory wire flags, verifying peer flags are preserved and only self is derived. | Pass |
| T12.5-06 | unit (frontend — `phase12.behavior.test.tsx`) | Host badge rendering visually identical | Render both roster views and assert exactly one accessible host badge. | Pass |

---

## VP-12.6 Fix WebRTC Sync-Peers Race

*(BL-WEBRTC-SYNCPEERS-RACE-01)*

### Implementation Matrix

| Subtask | Task | Module / Interface | Detail | Status | Pass Criteria | Evidence Pointer |
|---------|------|--------------------|--------|--------|---------------|-----------------|
| 12.6.1 | Flush peer repair from committed state | `frontend/src/features/room/useVaporRoom.ts` | Keep `withPeerLeft` derivation pure; set a pending ref before the update and flush once from an effect keyed on committed participants. Clear pending before synchronization. | Implemented | No WebRTC/ref/log side effects occur inside the updater; batched and StrictMode flows synchronize once from the final committed set. | `useVaporRoom.ts` |

### Test Plan

**Execution status: Pass — fresh full-suite rerun confirmed 2026-07-30.** All mapped Phase 12 tests pass; see `docs/test_results/phase-12-test-result.md`.

| Test # | Suite | Purpose | Verification Focus | Status |
|--------|-------|---------|-------------------|--------|
| T12.6-01 | unit (frontend — `phase12.behavior.test.tsx`) | Race: `peer_left` before `peer_joined` commit must not prune the new peer | Render the hook, synchronously drive captured peer-join/peer-left socket handlers, then observe state and `syncPeers` arguments. | Pass |
| T12.6-02 | unit (frontend — `phase12.behavior.test.tsx`) | Commit-phase idempotency under StrictMode and batching (I-12.6b) | Render under `React.StrictMode`; drive last-peer and multiple batched leaves; assert zero/one mesh calls respectively and the final committed peer set. | Pass |
| T12.6-03 | unit (frontend — `peerLeft.unit.test.mjs`) | Genuinely departed peers still pruned (I-12.6a) | Three-peer room; `peer_left` for one; assert `syncPeers` set omits the leaver and the mesh mock's `removePeer` fires for it (via `syncPeers` prune plus the preceding `handlePeerLeft`); guest↔guest repair offers still initiated per VP-10.2 semantics. | Pass |
| T12.6-04 | unit (frontend — regression) | Zero-remaining guard and last-peer teardown unchanged | `peer_left` reducing the room to self only: `syncPeers` not called (length-0 guard), pending messages cleared, `connectedPeerCount → 0`, `chatConnectionState → 'idle'`. | Pass |
| T12.6-05 | unit (frontend — regression, I-12.6c) | Rest of `onPeerLeft` order/behavior unchanged | System chat message text for `disconnect`/`leave`/`kick` reasons, `soloDeadlineAt` adoption from payload, `chatConnectionState → 'connecting'` when peers remain but none connected — all identical to pre-change assertions; typing-state cleanup still triggered via `handlePeerLeft → removePeer → onRemoteTypingStatus(false)`. | Pass |
| T12.6-06 | integration (existing — frontend `webrtc.integration.test.mjs`) | No regression in mesh repair suites | All existing VP-10.2 sync/repair tests pass without modification. | Pass |

---

## VP-12.7 Fix Closed Data Channel Reuse

*(BL-WEBRTC-CLOSED-CHANNEL-REUSE-01)*

### Implementation Matrix

| Subtask | Task | Module / Interface | Detail | Status | Pass Criteria | Evidence Pointer |
|---------|------|--------------------|--------|--------|---------------|-----------------|
| 12.7.1 | Evict dead channels in `startOffer` before the duplicate-guard | `frontend/src/features/room/webrtc-chat-mesh.ts:367-394` | At the top of `startOffer`, before the `!this.dataChannels.has(peerId)` guard (`:370`): `const stale = this.dataChannels.get(peerId); if (stale && (stale.readyState === 'closed' \|\| stale.readyState === 'closing')) { null out onopen/onclose/onerror/onmessage; stale.close(); this.dataChannels.delete(peerId) }`. Do **not** call `removePeer` — the `RTCPeerConnection` must survive (Constraints #25). `'open'`/`'connecting'` entries untouched (I-12.7a); `needsOffer`, `emitConnectedPeerCount`, `shouldInitiate` unchanged (I-12.7b). | Implemented | A repair-pass `startOffer` against a peer with a closed channel creates a fresh channel and the offer carries it; healthy channels never duplicated; typecheck clean. | `webrtc-chat-mesh.ts:367-394,99-111,404-437` |

### Test Plan

**Execution status: Pass — fresh full-suite rerun confirmed 2026-07-30.** All mapped Phase 12 tests pass; see `docs/test_results/phase-12-test-result.md`.

| Test # | Suite | Purpose | Verification Focus | Status |
|--------|-------|---------|-------------------|--------|
| T12.7-01 | unit (frontend — `phase12.behavior.test.tsx`, mock RTC) | `'closed'` channel: evicted, fresh channel created | Execute `syncPeers` against a seeded mock closed channel; assert handler cleanup, replacement map entry, and relayed offer. | Pass |
| T12.7-02 | unit (frontend) | `'closing'` channel: same eviction path | As T12.7-01 with `readyState: 'closing'`; assert eviction + fresh channel; `close()` on the closing channel tolerated. | Pass |
| T12.7-03 | unit (frontend) | `'open'` channel: guard holds (I-12.7a) | Seed `'open'` channel; `startOffer`; assert `createDataChannel` **not** called, existing channel retained, offer still created against the existing connection. | Pass |
| T12.7-04 | unit (frontend) | `'connecting'` channel: guard holds (I-12.7a) | Seed `'connecting'` channel; `startOffer`; assert no eviction, no new channel. | Pass |
| T12.7-05 | unit (frontend) | No channel present: baseline creation unchanged | Empty map; `startOffer`; assert exactly one `createDataChannel` + attach (pre-existing behavior regression pin). | Pass |
| T12.7-06 | unit (frontend) | `connectedPeerCount` and politeness unchanged (I-12.7b) | After a closed-channel eviction + recreate, `emitConnectedPeerCount` counts only `'open'` channels (evicted channel no longer counted at any state); `shouldInitiate` lexicographic rule and `needsOffer`'s open/connecting short-circuit produce identical decisions to pre-change for all readyState inputs. | Pass |
| T12.7-07 | unit (frontend — end-to-end repair scenario) | Regression for the original bug: silent `onclose` then repair pass restores messaging | Simulate channel `onclose` firing without connection failure (channel left `'closed'` in map, connection healthy/stable); run `syncPeers([peer])`; assert `needsOffer` true → `startOffer` evicts and recreates → after mock channel opens, `sendMessage` delivers (deliveredCount 1). | Pass |

---

## VP-12.8 Fix Timer Double-Countdown

*(BL-UX-TIMER-DOUBLE-COUNTDOWN-01)*

### Implementation Matrix

| Subtask | Task | Module / Interface | Detail | Status | Pass Criteria | Evidence Pointer |
|---------|------|--------------------|--------|--------|---------------|-----------------|
| 12.8.1 | Remove the deadline `Math.min`; pass room TTL through | `frontend/src/features/room/useVaporRoom.ts:656-661,708` | Deleted the `effectiveExpiresAt` `useMemo`; `derived.expiresAt` now receives `state.expiresAt` directly. `derived.soloDeadlineAt`, `getLifetimeText`, and `getSoloWaitingText` are unchanged. | Done | `RoomLifetimeChip` receives only `state.expiresAt`; solo/grace deadlines can no longer influence the "Ends in" number; typecheck + lint clean. | `useVaporRoom.ts:701-755` |
| 12.8.2 | Add the normative host-grace countdown | `HostReconnectGraceBanner.tsx`, `App.tsx`, `RoomView.tsx`, `RoomViewDesktop.tsx` | Pass the grace deadline to both layouts and render a dedicated, accessible, one-second banner independent of TTL/solo chips. | Done | Both layouts show the remaining `mm:ss`; absent/invalid/expired deadlines render nothing; host return removes the banner. | `HostReconnectGraceBanner.tsx`, `App.tsx`, both room views |
| 12.8.3 | Update the conflicting normative spec | `docs/system_design/lifecycle.md:86` (+ frontend-ui-spec.md timer-display prose if present) | Replaced the single-countdown rule with the one-deadline-one-widget rule. `frontend-ui-spec.md` was checked and already assigns separate TTL and host-grace UI surfaces. | Done | lifecycle.md §3 no longer mandates the min() derivation; no normative doc contradicts the shipped behavior. | `lifecycle.md:85-86`, `frontend-ui-spec.md:125-148` |
| 12.8.4 | Align the exact 10-minute TTL formatting boundary | `frontend/src/features/room/useVaporRoom.ts`, frontend timer tests | Changed the compact branch from `minutes >= 10` to `remainingMs > 10 * 60 * 1000`; exactly 10:00 now renders `Ends in 10:00`, while values above 10 minutes remain compact. | Done | Runtime tests cover above, at, and below 10 minutes; frontend-ui-spec.md §4/§9.5 and implementation agree. | `useVaporRoom.ts:getLifetimeText`, `phase12.behavior.test.tsx`, `contract.integration.test.mjs` |

### Test Plan

**Execution status: Pass — fresh full-suite rerun confirmed 2026-07-30.** All mapped Phase 12 tests pass; see `docs/test_results/phase-12-test-result.md`.

| Test # | Suite | Purpose | Verification Focus | Status |
|--------|-------|---------|-------------------|--------|
| T12.8-01 | unit (frontend — `phase12.behavior.test.tsx`) | TTL-only baseline unchanged: (`expiresAt`, null, null) | Execute timer formatter boundary values; the hook/state integration remains covered by the runtime socket/reducer tests. Source-text checks are supplemental only. | Pass |
| T12.8-02 | unit (frontend) | Solo active must not affect the lifetime chip: (`expiresAt`, `soloDeadlineAt` < `expiresAt`, null) | `derived.expiresAt === state.expiresAt` (not the smaller solo deadline); `derived.soloDeadlineAt === state.soloDeadlineAt`; the two chips show different countdowns (I-12.8a). | Pass |
| T12.8-03 | unit (frontend) | Grace active must not affect the lifetime chip: (`expiresAt`, null, `hostReconnectGraceDeadlineAt` < `expiresAt`) | `derived.expiresAt === state.expiresAt`; "Ends in" no longer jumps to the grace deadline on host disconnect. | Pass |
| T12.8-04 | unit (frontend) | All three non-null: (`expiresAt`, solo, grace) | `derived.expiresAt === state.expiresAt` regardless of which of the three is smallest; solo chip shows solo only. | Pass |
| T12.8-05 | unit (frontend) | `expiresAt` null with solo non-null: (null, solo, null) | `derived.expiresAt === null` → `RoomLifetimeChip` renders nothing (null handling matches today, I-12.8c); `SoloWaitingChip` still renders the solo countdown. | Pass |
| T12.8-06 | unit (frontend) | `expiresAt` null with grace non-null: (null, null, grace) and (null, solo, grace) | `derived.expiresAt === null` in both; no chip fabricates a countdown from grace/solo; grace surfaced only by the status line. | Pass |
| T12.8-07 | unit (frontend) | All null: (null, null, null) | `derived.expiresAt === null`, `derived.soloDeadlineAt === null`; both chips render nothing; no NaN/`Math.min()`-of-empty artifacts (memo deleted). | Pass |
| T12.8-08 | unit (frontend — `phase12.behavior.test.tsx`) | Host-grace countdown visible and isolated (I-12.8b) | Render both views, assert the dedicated accessible banner and tick from `10:00` to `09:59`; TTL remains null. | Pass |
| T12.8-09 | unit (frontend — regression) | `SoloWaitingChip` behavior byte-identical (I-12.8a) | `getSoloWaitingText` outputs for representative remaining times (>10 min, <10 min, expired, null) match pre-change snapshots; chip mounts/unmounts on the same conditions. | Pass |
| T12.8-10 | unit (frontend — regression) | Countdown cadence and normative formatting (I-12.8c) | `getLifetimeText` cases cover >10 min `Ends in Nm`, exactly 10 min `Ends in 10:00`, <10 min `mm:ss`, and expired → null; 1-second tick cadence in both chip components remains untouched. | Pass |
